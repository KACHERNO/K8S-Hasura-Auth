apiVersion: v1
kind: Service
metadata:
  labels:
    app: authserver
  name: authserver
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    app: authserver
  type: LoadBalancer
